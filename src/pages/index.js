import React from "react"
import Layout from "../components/Layout"

import style from "../styles/index.module.scss"

const Index = () => {
  return (
    <Layout>
      <h2>Homepage</h2>
      <p>This is the site landing page and will display the primary content banner for the event and all the important details about the event, location and what's being offered. Consider also including direct links to the three programs (for now, just linking them to the same page).</p>
    </Layout>
  )
}

export default Index
